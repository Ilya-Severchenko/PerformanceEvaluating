@model IEnumerable<PerformanceEvaluating.Data.Models.DomainRequestResult>

@{
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<div class="table-responsive">
    <table class="table table-striped table-bordered">
        <tr>
            <th>Url</th>
            @*<th>Response time</th>*@
            <th hidden>StatusCode</th>
        </tr>
        @foreach (var requestResult in Model)
        {
            <tr>
                <td>@requestResult.Url</td>
                @*<td>@requestResult.Attempt</td>*@
                <td hidden>@requestResult.StatusCode</td>
            </tr>
        }
    </table>
</div>